services:
  cc-adapter:
    image: dgqyushen/command-code-proxy:latest
    container_name: cc-adapter
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 512M
